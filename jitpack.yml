# JitPack builds the library from a git tag: implementation("com.github.JorgeLuisZB:uxtracker-android-sdk:<tag>")
jdk:
  - openjdk17
install:
  - ./gradlew :uxtracker:publishReleasePublicationToMavenLocal
